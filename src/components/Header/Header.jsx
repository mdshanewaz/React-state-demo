import { NavigationBar } from "./NavigationBar/NavigationBar";

export const Header = ({ user }) => {
    return (
        <header>
            <h2>Header</h2>
            < NavigationBar />
        </header>
    );
};